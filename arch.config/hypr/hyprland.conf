# UWSM applications
exec-once = systemctl --user start hyprpolkitagent.service
exec-once = hyprctl setcursor catppuccin-mocha-dark-cursors 28
exec-once = uwsm app -- waybar &
# exec = uwsm app -- hypridle &
# exec = uwsm app -- hyprpaper &
exec-once = uwsm app -- swayosd-server &
exec-once = ~/.local/bin/dynamic-borders.sh
exec = uwsm app -- /usr/local/bin/elephant &
exec = uwsm app -- /usr/local/bin/walker --gapplication-service &

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor = , highres@highrr, auto, 1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf
# Set programs that you use
$terminal = ghostty
$menu = walker


xwayland {
    force_zero_scaling = true
}

# Some default env vars.
env = XCURSOR_SIZE,8
env = QT_QPA_PLATFORMTHEME,qt6ct # change to qt6ct if you have that
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = NVD_BACKEND,direct
env = GDK_SCALE, 1
exec = gsettings set org.gnome.desktop.interface gtk-theme "Adwaita-dark"
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"
# install gnome-themes-extra; nws-look; qt6ct

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    # Change speed of keyboard repeat
    repeat_rate = 20
    repeat_delay = 400

    touchpad {
        natural_scroll = yes
    }

    accel_profile = none  # Can be flat, adaptive, or none.
    force_no_accel = 1         # 0 enables acceleration, 1 disables it.
    sensitivity = 0.35      # Range -1.0 to 1.0, where 0 means no change.
    follow_mouse = 1

    scroll_method = on_button_down
    scroll_button = 274
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    
    border_size = 1

    # col.active_border = $mauve $flamingo 90deg
    # col.inactive_border = $subtext0
    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)
    col.active_border = rgba(66bcbbaa) rgba(88aaaacc) 45deg
    col.inactive_border = rgba(88888877)

    resize_on_border = true

    gaps_in = 2
    gaps_out = 4

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    rounding = 0

    shadow {
        enabled = true
        range = 2
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 3
        vibrancy = 0.1696
    }

    # drop_shadow = yes
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 0, 0, ease
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

# master {
#     # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
#     new_is_master = true
# }

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true
    disable_splash_rendering  = true
    focus_on_activate = true
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
layerrule = noanim, wofi
# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# Browser types
windowrule = tag +chromium-based-browser, class:([cC]hrom(e|ium)|[bB]rave-browser|Microsoft-edge|Vivaldi-stable)
windowrule = tag +firefox-based-browser, class:(Firefox|zen|librewolf)

# Force chromium-based browsers into a tile to deal with --app bug
windowrule = tile, tag:chromium-based-browser

# Only a subtle opacity change, but not for video sites
windowrule = opacity 1 0.97, tag:chromium-based-browser
windowrule = opacity 1 0.97, tag:firefox-based-browser

# Some video sites should never have opacity applied to them
windowrule = opacity 1.0 1.0, initialTitle:(youtube\.com_/|app\.zoom\.us_/wc/home)

# Floating windows
windowrule = float, tag:floating-window
windowrule = center, tag:floating-window
windowrule = size 800 600, tag:floating-window

windowrule = tag +floating-window, class:(blueberry.py|Impala|Wiremix|org.gnome.NautilusPreviewer|com.gabm.satty|Omarchy|About|TUI.float)
windowrule = tag +floating-window, class:(xdg-desktop-portal-gtk|sublime_text|DesktopEditors|org.gnome.Nautilus), title:^(Open.*Files?|Open Folder|Save.*Files?|Save.*As|Save|All Files)

# Fullscreen screensaver
windowrule = fullscreen, class:Screensaver

# No transparency on media windows
windowrule = opacity 1 1, class:^(zoom|vlc|mpv|org.kde.kdenlive|com.obsproject.Studio|com.github.PintaProject.Pinta|imv|org.gnome.NautilusPreviewer)$

# Application-specific animation
layerrule = noanim, walker

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = ALT

binds {
    # workspace_back_and_forth = true
    binds = ALT_R, E, workspace, previous
}

# Only display the OSD on the currently focused monitor
$osdclient = swayosd-client --monitor "$(hyprctl monitors -j | jq -r '.[] | select(.focused == true).name')"

# Desktop multimedia keys for volume and LCD brightness (with OSD)
bindid = ,XF86AudioRaiseVolume, Volume up, exec, $osdclient --output-volume raise
bindid = ,XF86AudioLowerVolume, Volume down, exec, $osdclient --output-volume lower
bindid = ,XF86AudioMute, Mute, exec, $osdclient --output-volume mute-toggle
bindid = ,XF86AudioMicMute, Mute microphone, exec, $osdclient --input-volume mute-toggle
bindid = ,XF86MonBrightnessUp, Brightness up, exec, $osdclient --brightness raise
bindid = ,XF86MonBrightnessDown, Brightness down, exec, $osdclient --brightness lower
binds = ALT_R, Up, exec, $osdclient --output-volume 2
binds = ALT_R, Down, exec, $osdclient --output-volume -2

# Requires playerctl
bindid = , XF86AudioNext, Next track, exec, $osdclient --playerctl next
bindid = , XF86AudioPause, Pause, exec, $osdclient --playerctl play-pause
bindid = , XF86AudioPlay, Play, exec, $osdclient --playerctl play-pause
bindid = , XF86AudioPrev, Previous track, exec, $osdclient --playerctl previous

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
binds = ALT_R&SHIFT_R, Q, killactive
bind = $mainMod, M, exit
binds = ALT_R, V, togglefloating
bind = $mainMod, space, exec, $menu
bind = SUPER_SHIFT, l, exec, hyprlock
bind = SUPER_SHIFT, R, exec, hyprctl reload
bind = SUPER_SHIFT, S, exec, hyprshot -m window
bind = CTRL_SHIFT, S, exec, hyprshot -m region
# bind = $mainMod, P, pseudo, # dwindle
bind = SUPER, E, togglesplit, # dwindle
bind = $mainMod, F, fullscreen, # dwindle
# bind = $mainMod, W, togglegroup, # dwindle

# Move focus with mainMod + arrow keys
bind = SUPER, h, movefocus, l
bind = SUPER, l, movefocus, r
bind = SUPER, k, movefocus, u
bind = SUPER, j, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod_SHIFT, 1, movetoworkspace, 1
bind = $mainMod_SHIFT, 2, movetoworkspace, 2
bind = $mainMod_SHIFT, 3, movetoworkspace, 3
bind = $mainMod_SHIFT, 4, movetoworkspace, 4
bind = $mainMod_SHIFT, 5, movetoworkspace, 5
bind = $mainMod_SHIFT, 6, movetoworkspace, 6
bind = $mainMod_SHIFT, 7, movetoworkspace, 7
bind = $mainMod_SHIFT, 8, movetoworkspace, 8
bind = $mainMod_SHIFT, 9, movetoworkspace, 9
bind = $mainMod_SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod_SHIFT, S, movetoworkspace, special:magic

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
