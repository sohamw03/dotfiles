# apply all images from the wallpapers directory to every monitor
wallpaper "global" {
    monitor = ""
    path = "/home/soham/.config/hypr/wallpapers"
    fit_mode = "cover"    # contain, cover, tile, fit
    timeout = 1800          # seconds to rotate images (0 = static)
}

ipc = 1
splash = 0
